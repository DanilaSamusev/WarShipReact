/*handleClick(square) {

    fetch('https://localhost:5001/api/game',
        {
            method: 'put',
            headers:
                {
                    'Accept': 'application/json',
                    'Content-Type': 'application/json',
                },
            body: JSON.stringify(
                {
                    id: square.id,
                    isClicked: square.isClicked,
                })
        })
        .then(function (response) {
            return response.json()
        })
        .then((json) => console.log(square.id))

}*/

